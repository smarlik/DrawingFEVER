void draw(GraphicsContext gc) {
	for(int c=0; c<1000; c=c+1) {
	int y= rand(300);
	int x= rand(500);
	square(gc,x,y);
	sleep(1000);
	
	}
}

void 
square(GraphicsContext gc,int x, int y) {
	gc.strokeLine(x,y,x,y+35);
    gc.strokeLine(x,y,x+35,y);
    gc.strokeLine(x,y+35,x+35,y+35);
	gc.strokeLine(x+35,y,x+35,y+35);
}
/open launcher.java
























































































































































































































